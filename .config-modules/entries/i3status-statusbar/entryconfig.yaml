# File where the resulting static + chosen module will be exported to
export-to: "~/.config/i3status/config"

# If set to true, the module will come before the static
module-first: true

# An update command to run upon export
post-apply-hook: "i3-msg restart"
